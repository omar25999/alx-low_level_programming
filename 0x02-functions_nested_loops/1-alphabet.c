#include "main.h"
/**
 * print_alphabet - function which prints alphabets in lowercase
 *
 *
 * Return: Always 0
 */

void print_alphabet(void)
{
	char k;

	for (k = 'a'; k <= 'z'; k++)
		_putchar(k);
	_putchar('\n');
}
